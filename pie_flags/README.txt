get_puzzle.py
- used to fetch the images for the puzzle

get_wikipedia.py
- used to fetch the state/provice flag images from wikipedia

annotate_puzzle.py
- count the % of colors in the puzzle pie charts and annotate the
  images

find_by_color.py
- given some color hex codes, look for images that contain those hex
  colors
